---
phase: 04-habit-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00002_create_habits.sql
  - lib/features/habits/domain/habit_model.dart
  - lib/features/habits/domain/habit_log_model.dart
  - lib/features/habits/domain/habit_frequency.dart
  - lib/features/habits/data/habit_repository.dart
  - lib/features/habits/presentation/providers/habit_provider.dart
  - pubspec.yaml
  - test/unit/habits/habit_model_test.dart
  - test/unit/habits/habit_repository_test.dart
autonomous: true
requirements:
  - HABIT-01
must_haves:
  truths:
    - "Habit model can serialize to/from Supabase JSON with all fields (name, frequency, target_count, custom_days, icon)"
    - "HabitLog model can serialize to/from Supabase JSON with habit_id, completed_date (DATE), and count"
    - "HabitRepository provides getHabits, createHabit, updateHabit, deleteHabit, logCompletion, getLogs methods"
    - "Riverpod providers expose habit list as AsyncNotifier and repository as Provider"
  artifacts:
    - path: "supabase/migrations/00002_create_habits.sql"
      provides: "habits and habit_logs tables with RLS policies"
      contains: "create table public.habits"
    - path: "lib/features/habits/domain/habit_model.dart"
      provides: "Habit model with fromJson/toJson"
      contains: "class Habit"
    - path: "lib/features/habits/domain/habit_log_model.dart"
      provides: "HabitLog model with fromJson/toJson"
      contains: "class HabitLog"
    - path: "lib/features/habits/domain/habit_frequency.dart"
      provides: "HabitFrequency enum"
      contains: "enum HabitFrequency"
    - path: "lib/features/habits/data/habit_repository.dart"
      provides: "Supabase CRUD repository"
      exports: ["HabitRepository"]
    - path: "lib/features/habits/presentation/providers/habit_provider.dart"
      provides: "Riverpod providers for habit state"
      contains: "habitListProvider"
  key_links:
    - from: "lib/features/habits/data/habit_repository.dart"
      to: "lib/features/habits/domain/habit_model.dart"
      via: "Habit.fromJson in repository methods"
      pattern: "Habit\\.fromJson"
    - from: "lib/features/habits/presentation/providers/habit_provider.dart"
      to: "lib/features/habits/data/habit_repository.dart"
      via: "ref.read(habitRepositoryProvider)"
      pattern: "habitRepositoryProvider"
---

<objective>
Create the complete data foundation for habit tracking: Supabase migration (habits + habit_logs tables with RLS), domain models (Habit, HabitLog, HabitFrequency), repository with CRUD + log operations, Riverpod providers, and add fl_chart + contribution_heatmap dependencies.

Purpose: Establishes all data contracts and backend infrastructure that Plans 02 and 03 build UI against. Without this, no habit screen can fetch or persist data.
Output: Migration SQL, 3 domain files, repository, providers, updated pubspec.yaml, unit tests for model and repository.
</objective>

<execution_context>
@C:/Users/tolis-pc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tolis-pc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-habit-tracking/04-CONTEXT.md
@.planning/phases/04-habit-tracking/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from the existing codebase -->

From lib/features/profile/domain/profile_model.dart:
```dart
class Profile {
  final String id;
  final String? displayName;
  // ... fromJson/toJson pattern (excludes id and created_at from toJson)
  factory Profile.fromJson(Map<String, dynamic> json) { ... }
  Map<String, dynamic> toJson() => { ... };
}
```

From lib/features/profile/data/profile_repository.dart:
```dart
class ProfileRepository {
  ProfileRepository([SupabaseClient? client])
      : _client = client ?? Supabase.instance.client;
  final SupabaseClient _client;
  // Pattern: optional client param for DI in tests
}
```

From lib/features/auth/presentation/providers/auth_provider.dart:
```dart
// Riverpod 3.x patterns used in this project:
final authRepositoryProvider = Provider<AuthRepository>((ref) => AuthRepository());
final authStateProvider = NotifierProvider<AuthStateNotifier, AppAuthState>(AuthStateNotifier.new);
// Note: Project uses Notifier (not AsyncNotifier) for auth, but habits should use AsyncNotifier
// since data comes from async Supabase queries
```

From lib/core/utils/extensions.dart:
```dart
extension BuildContextX on BuildContext {
  ColorScheme get colorScheme => Theme.of(this).colorScheme;
  TextTheme get textTheme => Theme.of(this).textTheme;
}
```

From supabase/migrations/00001_create_profiles.sql:
```sql
-- RLS pattern: (select auth.uid()) = user_id
-- Trigger pattern exists for profiles auto-creation
-- habits uses same RLS approach but with user_id FK column instead of id = auth.uid()
```

From pubspec.yaml:
```yaml
dependencies:
  supabase_flutter: ^2.12.0
  flutter_riverpod: ^3.3.1
  # fl_chart and contribution_heatmap NOT yet added
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Supabase migration, domain models, and model tests</name>
  <files>
    supabase/migrations/00002_create_habits.sql,
    lib/features/habits/domain/habit_frequency.dart,
    lib/features/habits/domain/habit_model.dart,
    lib/features/habits/domain/habit_log_model.dart,
    test/unit/habits/habit_model_test.dart,
    pubspec.yaml
  </files>
  <read_first>
    supabase/migrations/00001_create_profiles.sql,
    lib/features/profile/domain/profile_model.dart,
    test/unit/profile/profile_repository_test.dart,
    pubspec.yaml
  </read_first>
  <behavior>
    - HabitFrequency enum has values: daily, weekly, custom
    - Habit.fromJson parses all fields: id, user_id, name, description, frequency, target_count, custom_days, icon, created_at, updated_at
    - Habit.fromJson handles null optionals (description, custom_days, icon) gracefully
    - Habit.toJson excludes id and created_at (server-managed), includes updated_at as DateTime.now().toIso8601String()
    - Habit.isBinary returns true when targetCount == 1, false otherwise
    - Habit.isCompletedToday returns true when todayProgress >= targetCount
    - Habit.copyWith creates new instance preserving unchanged fields
    - HabitLog.fromJson parses id, habit_id, completed_date (as DateTime), count, created_at
    - HabitLog.toJson produces habit_id, completed_date (as yyyy-MM-dd string), count
  </behavior>
  <action>
    1. Create `supabase/migrations/00002_create_habits.sql` with the exact SQL from RESEARCH.md:
       - `habits` table: id (uuid PK, gen_random_uuid()), user_id (uuid FK -> auth.users ON DELETE CASCADE), name (text NOT NULL), description (text), frequency (text NOT NULL DEFAULT 'daily' CHECK in ('daily','weekly','custom')), target_count (integer NOT NULL DEFAULT 1 CHECK >= 1), custom_days (integer[] DEFAULT NULL), icon (text), created_at (timestamptz DEFAULT now()), updated_at (timestamptz DEFAULT now())
       - `habit_logs` table: id (uuid PK, gen_random_uuid()), habit_id (uuid FK -> habits ON DELETE CASCADE), completed_date (date NOT NULL DEFAULT current_date), count (integer NOT NULL DEFAULT 1 CHECK >= 1), created_at (timestamptz DEFAULT now()), UNIQUE (habit_id, completed_date)
       - Enable RLS on both tables
       - RLS policies for habits: select/insert/update/delete using `(select auth.uid()) = user_id`
       - RLS policies for habit_logs: select/insert/update/delete using EXISTS subquery checking habits.user_id = auth.uid()
       - Indexes: idx_habits_user_id, idx_habit_logs_habit_id, idx_habit_logs_completed_date, idx_habit_logs_habit_date (composite)

    2. Create `lib/features/habits/domain/habit_frequency.dart`:
       ```dart
       enum HabitFrequency { daily, weekly, custom }
       ```

    3. Create `lib/features/habits/domain/habit_model.dart` following the Profile.fromJson/toJson pattern exactly:
       - Fields: id (String), userId (String), name (String), description (String?), frequency (HabitFrequency), targetCount (int, default 1), customDays (List<int>?), icon (String?), createdAt (DateTime), updatedAt (DateTime), currentStreak (int, default 0, transient), todayProgress (int, default 0, transient)
       - Getters: `bool get isBinary => targetCount == 1;` and `bool get isCompletedToday => todayProgress >= targetCount;`
       - fromJson: parse frequency via `HabitFrequency.values.byName(json['frequency'] as String)`, customDays via `(json['custom_days'] as List?)?.cast<int>()`, targetCount with `?? 1` fallback
       - toJson: include user_id, name, description, frequency (as `.name`), target_count, custom_days, icon, updated_at. EXCLUDE id and created_at.
       - copyWith method for all mutable fields

    4. Create `lib/features/habits/domain/habit_log_model.dart`:
       - Fields: id (String), habitId (String), completedDate (DateTime), count (int, default 1), createdAt (DateTime)
       - fromJson: parse completed_date via `DateTime.parse(json['completed_date'] as String)`, count with `?? 1` fallback
       - toJson: include habit_id, completed_date formatted as `yyyy-MM-dd` string (use padLeft for month/day), count. EXCLUDE id and created_at.

    5. Update `pubspec.yaml` to add under dependencies:
       ```yaml
       fl_chart: ^1.2.0
       contribution_heatmap: ^0.5.3
       ```

    6. Create `test/unit/habits/habit_model_test.dart` with tests for all behaviors listed above. Follow the exact test style from `test/unit/profile/profile_repository_test.dart` (group/test structure, expect matchers).
  </action>
  <verify>
    <automated>flutter test test/unit/habits/habit_model_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/00002_create_habits.sql contains `create table public.habits`
    - supabase/migrations/00002_create_habits.sql contains `create table public.habit_logs`
    - supabase/migrations/00002_create_habits.sql contains `unique (habit_id, completed_date)`
    - supabase/migrations/00002_create_habits.sql contains `enable row level security` (twice, once per table)
    - supabase/migrations/00002_create_habits.sql contains at least 8 `create policy` statements (4 per table)
    - lib/features/habits/domain/habit_frequency.dart contains `enum HabitFrequency { daily, weekly, custom }`
    - lib/features/habits/domain/habit_model.dart contains `class Habit`
    - lib/features/habits/domain/habit_model.dart contains `factory Habit.fromJson(Map<String, dynamic> json)`
    - lib/features/habits/domain/habit_model.dart contains `Map<String, dynamic> toJson()`
    - lib/features/habits/domain/habit_model.dart contains `bool get isBinary => targetCount == 1`
    - lib/features/habits/domain/habit_model.dart contains `bool get isCompletedToday => todayProgress >= targetCount`
    - lib/features/habits/domain/habit_log_model.dart contains `class HabitLog`
    - lib/features/habits/domain/habit_log_model.dart contains `factory HabitLog.fromJson`
    - pubspec.yaml contains `fl_chart: ^1.2.0`
    - pubspec.yaml contains `contribution_heatmap: ^0.5.3`
    - test/unit/habits/habit_model_test.dart contains at least 8 `test(` calls
  </acceptance_criteria>
  <done>Habit and HabitLog models serialize correctly to/from Supabase JSON, frequency enum works, migration SQL ready for deployment, pubspec updated with chart dependencies, all model tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Repository and Riverpod providers with tests</name>
  <files>
    lib/features/habits/data/habit_repository.dart,
    lib/features/habits/presentation/providers/habit_provider.dart,
    test/unit/habits/habit_repository_test.dart
  </files>
  <read_first>
    lib/features/habits/domain/habit_model.dart,
    lib/features/habits/domain/habit_log_model.dart,
    lib/features/habits/domain/habit_frequency.dart,
    lib/features/profile/data/profile_repository.dart,
    lib/features/profile/presentation/providers/profile_provider.dart,
    lib/features/auth/presentation/providers/auth_provider.dart,
    test/helpers/test_helpers.dart
  </read_first>
  <action>
    1. Create `lib/features/habits/data/habit_repository.dart` following the ProfileRepository pattern:
       - Constructor: `HabitRepository([SupabaseClient? client]) : _client = client ?? Supabase.instance.client;`
       - Private `_client` field, `String get _userId => _client.auth.currentUser!.id;`
       - Methods:
         - `Future<List<Habit>> getHabits()` -- select from 'habits' where user_id = _userId, order by created_at, map via Habit.fromJson
         - `Future<Habit> getHabit(String habitId)` -- select single from 'habits' where id = habitId, return Habit.fromJson
         - `Future<void> createHabit(Habit habit)` -- insert habit.toJson() into 'habits'
         - `Future<void> updateHabit(Habit habit)` -- update habit.toJson() in 'habits' where id = habit.id
         - `Future<void> deleteHabit(String habitId)` -- delete from 'habits' where id = habitId
         - `Future<void> logCompletion(String habitId, {int count = 1})` -- check if entry exists for today (maybeSingle query on habit_id + completed_date as yyyy-MM-dd string using DateTime.now().toLocal()). If exists: update count to currentCount + count. If not: insert new row with habit_id, completed_date, count.
         - `Future<List<HabitLog>> getLogs(String habitId, {required DateTime from, required DateTime to})` -- select from 'habit_logs' where habit_id = habitId, completed_date >= from (yyyy-MM-dd), completed_date <= to (yyyy-MM-dd), order by completed_date, map via HabitLog.fromJson
         - `Future<int> getTodayProgress(String habitId)` -- select single log for habit_id + today's date (maybeSingle), return log['count'] or 0 if null

    2. Create `lib/features/habits/presentation/providers/habit_provider.dart`:
       - `final habitRepositoryProvider = Provider<HabitRepository>((ref) => HabitRepository());`
       - `final habitListProvider = AsyncNotifierProvider<HabitListNotifier, List<Habit>>(HabitListNotifier.new);`
       - HabitListNotifier extends AsyncNotifier<List<Habit>>:
         - `late final HabitRepository _repository;`
         - `build()`: reads habitRepositoryProvider, calls _repository.getHabits(), then for each habit fetches todayProgress via _repository.getTodayProgress(habit.id) and getLogs for streak calculation (delegate streak to a static method or compute from recent logs). Returns list with transient fields populated.
         - `createHabit(Habit habit)`: set loading, guard { create + refetch list }
         - `updateHabit(Habit habit)`: set loading, guard { update + refetch list }
         - `deleteHabit(String habitId)`: set loading, guard { delete + refetch list }
         - `checkIn(String habitId, {int count = 1})`: await logCompletion, then ref.invalidateSelf() to refresh
       - `final habitDetailProvider = FutureProvider.family<Habit, String>((ref, habitId) async { ... });` -- fetches single habit with today progress

    3. Create `test/unit/habits/habit_repository_test.dart`:
       - Test HabitRepository method signatures exist and return correct types
       - Test logCompletion date formatting produces yyyy-MM-dd format
       - Use the same test style as existing tests (group/test structure)
       - Since Supabase cannot be mocked easily without mockito codegen, test the date formatting utility and model integration
  </action>
  <verify>
    <automated>flutter test test/unit/habits/</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/habits/data/habit_repository.dart contains `class HabitRepository`
    - lib/features/habits/data/habit_repository.dart contains `Future<List<Habit>> getHabits()`
    - lib/features/habits/data/habit_repository.dart contains `Future<void> createHabit(Habit habit)`
    - lib/features/habits/data/habit_repository.dart contains `Future<void> updateHabit(Habit habit)`
    - lib/features/habits/data/habit_repository.dart contains `Future<void> deleteHabit(String habitId)`
    - lib/features/habits/data/habit_repository.dart contains `Future<void> logCompletion(String habitId`
    - lib/features/habits/data/habit_repository.dart contains `Future<List<HabitLog>> getLogs(String habitId`
    - lib/features/habits/data/habit_repository.dart contains `Supabase.instance.client`
    - lib/features/habits/presentation/providers/habit_provider.dart contains `habitRepositoryProvider`
    - lib/features/habits/presentation/providers/habit_provider.dart contains `habitListProvider`
    - lib/features/habits/presentation/providers/habit_provider.dart contains `AsyncNotifier<List<Habit>>`
    - lib/features/habits/presentation/providers/habit_provider.dart contains `checkIn(`
    - test/unit/habits/habit_repository_test.dart exists and contains at least 3 `test(` calls
  </acceptance_criteria>
  <done>HabitRepository provides complete Supabase CRUD + log operations, Riverpod providers expose async habit list state with check-in support, all unit tests pass</done>
</task>

</tasks>

<verification>
- `flutter test test/unit/habits/` runs all habit model and repository tests
- `grep -r "class Habit " lib/features/habits/` finds Habit model
- `grep -r "class HabitLog" lib/features/habits/` finds HabitLog model
- `grep -r "class HabitRepository" lib/features/habits/` finds repository
- `grep -r "habitListProvider" lib/features/habits/` finds provider
- pubspec.yaml includes fl_chart and contribution_heatmap
- Migration file exists at supabase/migrations/00002_create_habits.sql
</verification>

<success_criteria>
- All domain models (Habit, HabitLog, HabitFrequency) compile and serialize correctly
- Repository provides full CRUD + log + getLogs methods
- Riverpod providers wire repository to UI layer with AsyncNotifier pattern
- Supabase migration SQL is ready for deployment
- fl_chart and contribution_heatmap added to pubspec.yaml
- All unit tests in test/unit/habits/ pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-habit-tracking/04-01-SUMMARY.md`
</output>
