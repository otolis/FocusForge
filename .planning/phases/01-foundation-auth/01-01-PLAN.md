---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/core/constants/supabase_constants.dart
  - lib/core/theme/app_theme.dart
  - lib/core/theme/color_schemes.dart
  - lib/core/theme/text_theme.dart
  - lib/core/utils/extensions.dart
  - lib/features/settings/presentation/providers/theme_provider.dart
  - lib/shared/widgets/app_button.dart
  - lib/shared/widgets/app_text_field.dart
  - lib/shared/widgets/loading_overlay.dart
  - test/helpers/test_helpers.dart
  - test/unit/settings/theme_provider_test.dart
  - .gitignore
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "App launches without errors with ProviderScope and Supabase initialized"
    - "Light mode uses amber seed (#FF8F00) with cream surfaces (#FFFBF5) and teal tertiary (#2E7D6F)"
    - "Dark mode uses amber seed (#FF8F00) with warm charcoal surfaces (#1E1B16) and sage tertiary (#81C784)"
    - "Theme mode persists across app restarts via SharedPreferences"
    - "Typography uses Nunito for headings and Inter for body text"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies declared"
      contains: "supabase_flutter"
    - path: "lib/main.dart"
      provides: "App entry point with Supabase init"
      contains: "Supabase.initialize"
    - path: "lib/core/theme/color_schemes.dart"
      provides: "Light and dark ColorScheme definitions"
      contains: "ColorScheme.fromSeed"
    - path: "lib/features/settings/presentation/providers/theme_provider.dart"
      provides: "Theme mode state management with persistence"
      contains: "ThemeNotifier"
    - path: "test/helpers/test_helpers.dart"
      provides: "Shared test mocks and helpers"
      contains: "MockSupabaseClient"
  key_links:
    - from: "lib/app.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "ThemeData references"
      pattern: "AppTheme\\.light|AppTheme\\.dark"
    - from: "lib/app.dart"
      to: "lib/features/settings/presentation/providers/theme_provider.dart"
      via: "Riverpod consumer for theme mode"
      pattern: "themeProvider|ThemeMode"
    - from: "lib/main.dart"
      to: "lib/core/constants/supabase_constants.dart"
      via: "Supabase URL and anon key"
      pattern: "SupabaseConstants\\.url|SupabaseConstants\\.anonKey"
---

<objective>
Create the Flutter project scaffold with all Phase 1 dependencies, Material 3 theming system with amber seed color and light/dark/system mode, shared reusable widgets, and Wave 0 test infrastructure.

Purpose: Establishes the foundation that all subsequent plans build upon. Nothing can proceed without the project existing, dependencies installed, theming configured, and test infrastructure ready.
Output: Runnable Flutter app with themed MaterialApp, shared widgets, and test scaffolds.
</objective>

<execution_context>
@C:/Users/tolis-pc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tolis-pc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter project scaffold with all dependencies and Supabase initialization</name>
  <files>
    pubspec.yaml,
    lib/main.dart,
    lib/app.dart,
    lib/core/constants/supabase_constants.dart,
    lib/core/utils/extensions.dart,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-RESEARCH.md,
    .planning/phases/01-foundation-auth/01-UI-SPEC.md,
    CLAUDE.md
  </read_first>
  <action>
1. Create Flutter project (if not yet created):
   ```
   flutter create --org com.focusforge --project-name focusforge .
   ```
   If the project already exists (pubspec.yaml present), skip creation.

2. Update `pubspec.yaml` — add ALL Phase 1 dependencies under `dependencies`:
   - supabase_flutter: ^2.12.0
   - flutter_riverpod: ^3.3.1
   - go_router: ^17.1.0
   - google_sign_in: ^7.2.0
   - google_fonts: ^8.0.2
   - shared_preferences: ^2.5.4
   - image_picker: ^1.2.1
   - smooth_page_indicator: ^2.0.1
   - flutter_svg: ^2.2.4
   Under `dev_dependencies`:
   - mockito: ^5.4.4
   - build_runner: ^2.4.13
   - riverpod_lint: ^3.1.3

3. Create `lib/core/constants/supabase_constants.dart`:
   ```dart
   class SupabaseConstants {
     static const String url = 'YOUR_SUPABASE_URL';
     static const String anonKey = 'YOUR_SUPABASE_ANON_KEY';
   }
   ```
   Add `lib/core/constants/supabase_constants.dart` to `.gitignore` so secrets are never committed.
   Also create `lib/core/constants/supabase_constants.dart.example` with the same structure but placeholder values, so other developers know what to create.

4. Create `lib/main.dart`:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:supabase_flutter/supabase_flutter.dart';
   import 'core/constants/supabase_constants.dart';
   import 'app.dart';

   Future<void> main() async {
     WidgetsFlutterBinding.ensureInitialized();
     await Supabase.initialize(
       url: SupabaseConstants.url,
       anonKey: SupabaseConstants.anonKey,
     );
     runApp(const ProviderScope(child: FocusForgeApp()));
   }
   ```

5. Create `lib/app.dart` — a `ConsumerWidget` that:
   - Reads `themeProvider` for current `ThemeMode`
   - Returns `MaterialApp.router` (router will be wired in Plan 02, for now use `MaterialApp` with a placeholder home)
   - Sets `theme: AppTheme.light()`, `darkTheme: AppTheme.dark()`, `themeMode: ref.watch(themeProvider)`
   - Sets `debugShowCheckedModeBanner: false`
   - Title: 'FocusForge'

6. Create `lib/core/utils/extensions.dart` with a `BuildContext` extension:
   ```dart
   extension BuildContextX on BuildContext {
     ColorScheme get colorScheme => Theme.of(this).colorScheme;
     TextTheme get textTheme => Theme.of(this).textTheme;
   }
   ```

7. Run `flutter pub get` to install all dependencies.
  </action>
  <verify>
    <automated>cd "C:/Users/tolis-pc/Documents/repos/FocusForge" && flutter pub get && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `supabase_flutter: ^2.12.0`
    - pubspec.yaml contains `flutter_riverpod: ^3.3.1`
    - pubspec.yaml contains `go_router: ^17.1.0`
    - pubspec.yaml contains `google_sign_in: ^7.2.0`
    - pubspec.yaml contains `google_fonts: ^8.0.2`
    - pubspec.yaml contains `shared_preferences: ^2.5.4`
    - pubspec.yaml contains `mockito:` under dev_dependencies
    - lib/main.dart contains `Supabase.initialize`
    - lib/main.dart contains `ProviderScope`
    - lib/app.dart contains `FocusForgeApp`
    - lib/app.dart contains `AppTheme.light`
    - lib/app.dart contains `themeProvider`
    - lib/core/constants/supabase_constants.dart contains `class SupabaseConstants`
    - .gitignore contains `supabase_constants.dart`
    - lib/core/utils/extensions.dart contains `extension BuildContextX`
    - `flutter pub get` exits 0
  </acceptance_criteria>
  <done>Flutter project exists with all Phase 1 dependencies installed, Supabase initialization in main.dart, and app.dart wired to theme system</done>
</task>

<task type="auto">
  <name>Task 2: Material 3 theme system with amber seed, dual color schemes, and typography</name>
  <files>
    lib/core/theme/app_theme.dart,
    lib/core/theme/color_schemes.dart,
    lib/core/theme/text_theme.dart,
    lib/features/settings/presentation/providers/theme_provider.dart
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-UI-SPEC.md,
    .planning/phases/01-foundation-auth/01-RESEARCH.md,
    lib/app.dart
  </read_first>
  <action>
1. Create `lib/core/theme/color_schemes.dart`:
   ```dart
   import 'package:flutter/material.dart';

   const Color seedColor = Color(0xFFFF8F00);

   final ColorScheme lightColorScheme = ColorScheme.fromSeed(
     seedColor: seedColor,
     brightness: Brightness.light,
     tertiary: const Color(0xFF2E7D6F),
     surface: const Color(0xFFFFFBF5),
   );

   final ColorScheme darkColorScheme = ColorScheme.fromSeed(
     seedColor: seedColor,
     brightness: Brightness.dark,
     tertiary: const Color(0xFF81C784),
     surface: const Color(0xFF1E1B16),
   );
   ```

2. Create `lib/core/theme/text_theme.dart`:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:google_fonts/google_fonts.dart';

   TextTheme createTextTheme() {
     final nunitoTheme = GoogleFonts.nunitoTextTheme();
     final interTheme = GoogleFonts.interTextTheme();

     return TextTheme(
       // Display — Nunito 28dp bold
       displayLarge: nunitoTheme.displayLarge?.copyWith(fontSize: 28, fontWeight: FontWeight.w700, height: 1.2),
       displayMedium: nunitoTheme.displayMedium?.copyWith(fontSize: 28, fontWeight: FontWeight.w700, height: 1.2),
       // Headline — Nunito 22dp bold
       headlineMedium: nunitoTheme.headlineMedium?.copyWith(fontSize: 22, fontWeight: FontWeight.w700, height: 1.3),
       headlineSmall: nunitoTheme.headlineSmall?.copyWith(fontSize: 22, fontWeight: FontWeight.w700, height: 1.3),
       // Body — Inter 16dp regular
       bodyLarge: interTheme.bodyLarge?.copyWith(fontSize: 16, fontWeight: FontWeight.w400, height: 1.5),
       bodyMedium: interTheme.bodyMedium?.copyWith(fontSize: 16, fontWeight: FontWeight.w400, height: 1.5),
       // Label — Inter 14dp regular
       labelLarge: interTheme.labelLarge?.copyWith(fontSize: 14, fontWeight: FontWeight.w400, height: 1.4),
       labelMedium: interTheme.labelMedium?.copyWith(fontSize: 14, fontWeight: FontWeight.w400, height: 1.4),
     );
   }
   ```

3. Create `lib/core/theme/app_theme.dart`:
   ```dart
   import 'package:flutter/material.dart';
   import 'color_schemes.dart';
   import 'text_theme.dart';

   class AppTheme {
     static ThemeData light() {
       return ThemeData(
         useMaterial3: true,
         colorScheme: lightColorScheme,
         textTheme: createTextTheme(),
         inputDecorationTheme: InputDecorationTheme(
           border: OutlineInputBorder(borderRadius: BorderRadius.circular(12)),
           contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 14),
         ),
         filledButtonTheme: FilledButtonThemeData(
           style: FilledButton.styleFrom(
             minimumSize: const Size(double.infinity, 48),
             shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
           ),
         ),
         outlinedButtonTheme: OutlinedButtonThemeData(
           style: OutlinedButton.styleFrom(
             minimumSize: const Size(double.infinity, 48),
             shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
           ),
         ),
         cardTheme: CardTheme(
           elevation: 0,
           shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
         ),
         chipTheme: ChipThemeData(
           shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)),
         ),
         bottomNavigationBarTheme: const BottomNavigationBarThemeData(
           elevation: 0,
         ),
       );
     }

     static ThemeData dark() {
       return ThemeData(
         useMaterial3: true,
         colorScheme: darkColorScheme,
         textTheme: createTextTheme(),
         inputDecorationTheme: InputDecorationTheme(
           border: OutlineInputBorder(borderRadius: BorderRadius.circular(12)),
           contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 14),
         ),
         filledButtonTheme: FilledButtonThemeData(
           style: FilledButton.styleFrom(
             minimumSize: const Size(double.infinity, 48),
             shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
           ),
         ),
         outlinedButtonTheme: OutlinedButtonThemeData(
           style: OutlinedButton.styleFrom(
             minimumSize: const Size(double.infinity, 48),
             shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
           ),
         ),
         cardTheme: CardTheme(
           elevation: 0,
           shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
         ),
         chipTheme: ChipThemeData(
           shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)),
         ),
         bottomNavigationBarTheme: const BottomNavigationBarThemeData(
           elevation: 0,
         ),
       );
     }
   }
   ```

4. Create `lib/features/settings/presentation/providers/theme_provider.dart`:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:shared_preferences/shared_preferences.dart';

   class ThemeNotifier extends StateNotifier<ThemeMode> {
     ThemeNotifier() : super(ThemeMode.system) {
       _loadTheme();
     }

     static const _key = 'theme_mode';

     Future<void> _loadTheme() async {
       final prefs = await SharedPreferences.getInstance();
       final themeIndex = prefs.getInt(_key) ?? 2; // default: system
       state = ThemeMode.values[themeIndex];
     }

     Future<void> setTheme(ThemeMode mode) async {
       state = mode;
       final prefs = await SharedPreferences.getInstance();
       await prefs.setInt(_key, mode.index);
     }
   }

   final themeProvider = StateNotifierProvider<ThemeNotifier, ThemeMode>(
     (ref) => ThemeNotifier(),
   );
   ```

5. Ensure `lib/app.dart` (from Task 1) correctly imports and uses `AppTheme.light()`, `AppTheme.dark()`, and watches `themeProvider`. If Task 1 left a stub, update it now to use real theme references.
  </action>
  <verify>
    <automated>cd "C:/Users/tolis-pc/Documents/repos/FocusForge" && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/theme/color_schemes.dart contains `Color(0xFFFF8F00)`
    - lib/core/theme/color_schemes.dart contains `Color(0xFF2E7D6F)`
    - lib/core/theme/color_schemes.dart contains `Color(0xFF81C784)`
    - lib/core/theme/color_schemes.dart contains `Color(0xFFFFFBF5)`
    - lib/core/theme/color_schemes.dart contains `Color(0xFF1E1B16)`
    - lib/core/theme/color_schemes.dart contains `ColorScheme.fromSeed`
    - lib/core/theme/text_theme.dart contains `GoogleFonts.nunitoTextTheme`
    - lib/core/theme/text_theme.dart contains `GoogleFonts.interTextTheme`
    - lib/core/theme/text_theme.dart contains `fontSize: 28`
    - lib/core/theme/text_theme.dart contains `fontSize: 22`
    - lib/core/theme/text_theme.dart contains `fontSize: 16`
    - lib/core/theme/text_theme.dart contains `fontSize: 14`
    - lib/core/theme/app_theme.dart contains `useMaterial3: true`
    - lib/core/theme/app_theme.dart contains `BorderRadius.circular(12)` (button/input radius)
    - lib/core/theme/app_theme.dart contains `BorderRadius.circular(16)` (card radius)
    - lib/features/settings/presentation/providers/theme_provider.dart contains `class ThemeNotifier extends StateNotifier<ThemeMode>`
    - lib/features/settings/presentation/providers/theme_provider.dart contains `themeProvider`
    - lib/features/settings/presentation/providers/theme_provider.dart contains `SharedPreferences`
    - `flutter analyze --no-fatal-infos` exits 0
  </acceptance_criteria>
  <done>Material 3 theme system fully configured with amber seed color, cream/charcoal surfaces, teal/sage accents, Nunito+Inter typography, rounded corners (12dp buttons, 16dp cards), and persistent theme mode provider</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 test infrastructure and shared widgets</name>
  <files>
    test/helpers/test_helpers.dart,
    test/unit/settings/theme_provider_test.dart,
    lib/shared/widgets/app_button.dart,
    lib/shared/widgets/app_text_field.dart,
    lib/shared/widgets/loading_overlay.dart
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-VALIDATION.md,
    .planning/phases/01-foundation-auth/01-UI-SPEC.md,
    lib/features/settings/presentation/providers/theme_provider.dart,
    lib/core/theme/app_theme.dart
  </read_first>
  <behavior>
    - Test 1: ThemeNotifier initial state is ThemeMode.system
    - Test 2: ThemeNotifier.setTheme(ThemeMode.dark) updates state to ThemeMode.dark
    - Test 3: ThemeNotifier.setTheme(ThemeMode.light) updates state to ThemeMode.light
    - Test 4: Theme mode persists to SharedPreferences (verify prefs.setInt called)
  </behavior>
  <action>
1. Create `test/helpers/test_helpers.dart`:
   - Create a mock SupabaseClient class (manual mock or mockito annotation)
   - Create a mock SharedPreferences using `SharedPreferences.setMockInitialValues({})`
   - Create helper function `createTestApp(Widget child)` that wraps a widget in `ProviderScope` + `MaterialApp` for widget testing
   - Create helper function `createMockAuthRepository()` that returns a mock auth repository

2. Create `test/unit/settings/theme_provider_test.dart`:
   - Test group: "ThemeNotifier"
   - Test: "initial state is ThemeMode.system"
   - Test: "setTheme updates state to dark"
   - Test: "setTheme updates state to light"
   - Test: "persists theme mode to SharedPreferences"
   - Use `SharedPreferences.setMockInitialValues({})` in setUp
   - Create ThemeNotifier directly (not via provider) for unit testing

3. Create `lib/shared/widgets/app_button.dart`:
   - `AppButton` widget — a reusable FilledButton with:
     - Required `onPressed` callback and `label` string
     - Optional `isLoading` bool (default false) — when true, shows CircularProgressIndicator(strokeWidth: 2, color: onPrimary, size: 20dp) instead of label, and disables the button
     - Optional `isOutlined` bool (default false) — switches to OutlinedButton style
     - Optional `isDestructive` bool (default false) — uses colorScheme.error for background
     - Full width by default (double.infinity), 48dp height, 12dp border radius
     - Uses `context.colorScheme` extension for colors

4. Create `lib/shared/widgets/app_text_field.dart`:
   - `AppTextField` widget — a reusable TextFormField with:
     - Required `label` string (used as labelText in InputDecoration)
     - Optional `controller`, `validator`, `obscureText`, `keyboardType`, `textInputAction`, `onFieldSubmitted`, `prefixIcon`, `suffixIcon`, `errorText`
     - OutlinedBorder with 12dp radius (inherits from theme)
     - Uses `context.textTheme` for text styling

5. Create `lib/shared/widgets/loading_overlay.dart`:
   - `LoadingOverlay` widget — a Stack that overlays a semi-transparent barrier with centered CircularProgressIndicator
   - Takes `isLoading` bool and `child` widget
   - When `isLoading` is true, shows the overlay on top of child and absorbs pointer events
   - When false, shows only the child

6. Run tests to verify green:
   ```
   flutter test test/unit/settings/theme_provider_test.dart
   ```
  </action>
  <verify>
    <automated>cd "C:/Users/tolis-pc/Documents/repos/FocusForge" && flutter test test/unit/settings/theme_provider_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/helpers/test_helpers.dart contains `createTestApp`
    - test/helpers/test_helpers.dart contains `SharedPreferences`
    - test/unit/settings/theme_provider_test.dart contains `ThemeNotifier`
    - test/unit/settings/theme_provider_test.dart contains `ThemeMode.system`
    - test/unit/settings/theme_provider_test.dart contains `ThemeMode.dark`
    - lib/shared/widgets/app_button.dart contains `class AppButton`
    - lib/shared/widgets/app_button.dart contains `isLoading`
    - lib/shared/widgets/app_button.dart contains `isDestructive`
    - lib/shared/widgets/app_text_field.dart contains `class AppTextField`
    - lib/shared/widgets/app_text_field.dart contains `TextFormField`
    - lib/shared/widgets/loading_overlay.dart contains `class LoadingOverlay`
    - lib/shared/widgets/loading_overlay.dart contains `AbsorbPointer`
    - `flutter test test/unit/settings/theme_provider_test.dart` exits 0
  </acceptance_criteria>
  <done>Wave 0 test infrastructure in place with shared mock helpers, theme provider tests passing green, and three reusable shared widgets (AppButton, AppTextField, LoadingOverlay) ready for auth and profile screens</done>
</task>

</tasks>

<verification>
1. `flutter pub get` completes without errors
2. `flutter analyze --no-fatal-infos` shows no errors
3. `flutter test test/unit/settings/` passes all tests
4. Project structure follows RESEARCH.md recommended layout
5. All theme values match UI-SPEC.md color, typography, and corner radius specifications
</verification>

<success_criteria>
- Flutter project runs (`flutter run` would launch, though may show placeholder screen)
- All pubspec.yaml dependencies resolve
- Material 3 theme with amber/#FF8F00 seed, cream/charcoal surfaces, teal/sage accents compiles
- Theme provider persists user preference to SharedPreferences
- Typography uses Nunito (headings) + Inter (body) via google_fonts
- Shared widgets (AppButton, AppTextField, LoadingOverlay) exist and compile
- Test infrastructure with mocks and helpers is ready for subsequent plans
- Theme provider tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
