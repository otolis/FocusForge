---
phase: 05-ai-daily-planner
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/features/planner/presentation/screens/planner_screen.dart
  - lib/features/planner/presentation/widgets/timeline_widget.dart
  - lib/features/planner/presentation/widgets/time_block_card.dart
  - lib/features/planner/presentation/widgets/hour_marker.dart
  - lib/features/planner/presentation/widgets/energy_zone_band.dart
  - lib/features/planner/presentation/widgets/empty_slot.dart
  - lib/features/planner/presentation/widgets/shimmer_timeline.dart
  - lib/features/planner/presentation/widgets/add_item_sheet.dart
  - lib/features/planner/presentation/widgets/regenerate_bar.dart
  - lib/core/router/app_router.dart
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "User can see a vertical scrollable timeline with hour markers from 6 AM to 10 PM"
    - "User can see energy zone background bands matching their peak/low hour preferences"
    - "User can see scheduled time blocks as proportionally-sized colored cards on the timeline"
    - "User can add plannable items via a bottom sheet with title, duration picker, and energy level selector"
    - "User can tap 'Plan My Day' to trigger AI schedule generation and see shimmer skeleton while loading"
    - "User can see 'Regenerate' option with optional constraint text input after initial generation"
    - "User sees friendly error message with retry button when Groq API fails"
    - "User can navigate to the planner via the bottom navigation bar"
    - "Empty timeline shows dotted placeholder slots with '+' to add items"
  artifacts:
    - path: "lib/features/planner/presentation/screens/planner_screen.dart"
      provides: "Main planner screen with FAB, timeline, and state management"
      contains: "class PlannerScreen"
    - path: "lib/features/planner/presentation/widgets/timeline_widget.dart"
      provides: "Vertical scrollable timeline container"
      contains: "class TimelineWidget"
    - path: "lib/features/planner/presentation/widgets/time_block_card.dart"
      provides: "Individual schedule block card"
      contains: "class TimeBlockCard"
    - path: "lib/features/planner/presentation/widgets/hour_marker.dart"
      provides: "Hour label on the left axis"
      contains: "class HourMarker"
    - path: "lib/features/planner/presentation/widgets/energy_zone_band.dart"
      provides: "Background color band for energy zones"
      contains: "class EnergyZoneBand"
    - path: "lib/features/planner/presentation/widgets/empty_slot.dart"
      provides: "Dotted placeholder with + button"
      contains: "class EmptySlot"
    - path: "lib/features/planner/presentation/widgets/shimmer_timeline.dart"
      provides: "Shimmer skeleton for loading state"
      contains: "class ShimmerTimeline"
    - path: "lib/features/planner/presentation/widgets/add_item_sheet.dart"
      provides: "Bottom sheet for adding plannable items"
      contains: "class AddItemSheet"
    - path: "lib/features/planner/presentation/widgets/regenerate_bar.dart"
      provides: "Regenerate button and constraint input"
      contains: "class RegenerateBar"
    - path: "lib/core/router/app_router.dart"
      provides: "Updated router with PlannerScreen"
      contains: "PlannerScreen"
  key_links:
    - from: "lib/features/planner/presentation/screens/planner_screen.dart"
      to: "lib/features/planner/presentation/providers/planner_provider.dart"
      via: "ref.watch(plannerProvider)"
      pattern: "plannerProvider"
    - from: "lib/features/planner/presentation/screens/planner_screen.dart"
      to: "lib/features/planner/presentation/providers/plannable_items_provider.dart"
      via: "ref.watch(plannableItemsProvider)"
      pattern: "plannableItemsProvider"
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/planner/presentation/screens/planner_screen.dart"
      via: "GoRoute /planner path"
      pattern: "PlannerScreen"
---

<objective>
Build the complete planner UI: timeline visualization with energy zones, time block cards, add-item bottom sheet, "Plan My Day" AI generation flow with shimmer loading and error handling, regenerate bar, and router wiring.

Purpose: Give users a visual daily planner they can populate with items and generate AI-optimized schedules. This plan delivers the primary user-facing value of Phase 5.
Output: Working planner screen accessible via bottom navigation, rendering timeline with blocks, supporting item management and AI generation.
</objective>

<execution_context>
@C:/Users/tolis-pc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tolis-pc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-daily-planner/05-CONTEXT.md
@.planning/phases/05-ai-daily-planner/05-RESEARCH.md
@.planning/phases/05-ai-daily-planner/05-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From lib/features/planner/domain/plannable_item_model.dart:
```dart
enum EnergyLevel { high, medium, low }

class PlannableItem {
  final String id;
  final String userId;
  final String title;
  final int durationMinutes;     // 15, 30, 45, 60, 90, or 120
  final EnergyLevel energyLevel;
  final DateTime planDate;
  final DateTime createdAt;
  factory PlannableItem.fromJson(Map<String, dynamic> json);
  Map<String, dynamic> toJson();
  Map<String, dynamic> toEdgeFunctionJson();
  PlannableItem copyWith({...});
}
```

From lib/features/planner/domain/schedule_block_model.dart:
```dart
class ScheduleBlock {
  final String itemId;
  final String title;
  final int startMinute;       // Minutes from midnight (e.g., 540 = 9:00 AM)
  final int durationMinutes;
  final EnergyLevel energyLevel;
  int get endMinute;
  double get topOffset;        // Pixel offset from timeline top
  double get height;           // Pixel height
  factory ScheduleBlock.fromJson(Map<String, dynamic> json);
  Map<String, dynamic> toJson();
  ScheduleBlock copyWith({int? startMinute});
}
```

From lib/features/planner/domain/timeline_constants.dart:
```dart
enum EnergyZone { peak, low, regular }

class TimelineConstants {
  static const int startHour = 6;
  static const int endHour = 22;
  static const int totalHours = 16;
  static const double hourHeight = 80.0;
  static const double totalHeight = 1280.0;
  static const double pixelsPerMinute = hourHeight / 60.0;
  static const int snapMinutes = 15;
  static const double snapHeight = snapMinutes * pixelsPerMinute;
  static double minuteToY(int minute);
  static int yToSnappedMinute(double y);
  static EnergyZone getZone(int hour, EnergyPattern pattern);
  static List<ScheduleBlock> resolveOverlaps(List<ScheduleBlock> blocks);
}
```

From lib/features/planner/presentation/providers/plannable_items_provider.dart:
```dart
final plannerRepositoryProvider = Provider<PlannerRepository>((ref) => PlannerRepository());
final plannableItemsProvider = StateNotifierProvider.family<PlannableItemsNotifier, AsyncValue<List<PlannableItem>>, String>(...);
// PlannableItemsNotifier: loadItems(), addItem(), deleteItem(), updateItem(), setDate(), selectedDate
```

From lib/features/planner/presentation/providers/planner_provider.dart:
```dart
class PlannerState {
  final List<ScheduleBlock> blocks;
  final bool isGenerating;
  final String? error;
  final String? constraintsText;
}
final plannerProvider = StateNotifierProvider.family<PlannerNotifier, PlannerState, String>(...);
// PlannerNotifier: generateSchedule(), loadCachedSchedule(), moveBlock(), updateConstraints(), saveCurrentSchedule()
```

From lib/features/profile/domain/profile_model.dart:
```dart
class EnergyPattern {
  final List<int> peakHours;
  final List<int> lowHours;
  factory EnergyPattern.fromJson(Map<String, dynamic>? json);
  Map<String, dynamic> toJson();
}
```

From lib/core/theme/color_schemes.dart:
```dart
const Color seedColor = Color(0xFFFF8F00);  // Amber
// lightColorScheme.tertiary = Color(0xFF2E7D6F)  // Teal
// darkColorScheme.tertiary = Color(0xFF81C784)   // Sage green
```

From lib/shared/widgets/:
```dart
class AppButton extends StatelessWidget { ... }
class AppTextField extends StatelessWidget { ... }
class LoadingOverlay extends StatelessWidget { ... }
```

From lib/core/router/app_router.dart (line 103-106):
```dart
GoRoute(
  path: '/planner',
  builder: (context, state) => const PlaceholderTab(title: 'Planner'),
),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeline sub-widgets (hour markers, energy zones, time block cards, empty slots, shimmer)</name>
  <files>
    lib/features/planner/presentation/widgets/hour_marker.dart,
    lib/features/planner/presentation/widgets/energy_zone_band.dart,
    lib/features/planner/presentation/widgets/time_block_card.dart,
    lib/features/planner/presentation/widgets/empty_slot.dart,
    lib/features/planner/presentation/widgets/shimmer_timeline.dart
  </files>
  <read_first>
    lib/features/planner/domain/timeline_constants.dart,
    lib/features/planner/domain/schedule_block_model.dart,
    lib/features/planner/domain/plannable_item_model.dart,
    lib/core/theme/color_schemes.dart,
    lib/core/theme/app_theme.dart,
    lib/core/utils/extensions.dart,
    lib/features/profile/presentation/widgets/energy_prefs_picker.dart
  </read_first>
  <action>
Create 5 widget files. All use `context.colorScheme` and `context.textTheme` extensions from extensions.dart for theme access.

**1. `lib/features/planner/presentation/widgets/hour_marker.dart`**:

Stateless widget `HourMarker` taking `int hour` (6-21).

Renders a Row:
- Left side: `SizedBox(width: 48)` containing a Text label. Format hour: if hour == 0, "12 AM"; if hour < 12, "${hour} AM"; if hour == 12, "12 PM"; else "${hour - 12} PM". Style: `context.textTheme.labelSmall` with `context.colorScheme.onSurfaceVariant`.
- Right side: `Expanded` with a `Divider` (thin horizontal line, color `context.colorScheme.outlineVariant.withOpacity(0.3)`).

Total height: `TimelineConstants.hourHeight` (80px). The marker sits at the top of each hour block.

**2. `lib/features/planner/presentation/widgets/energy_zone_band.dart`**:

Stateless widget `EnergyZoneBand` taking `EnergyZone zone` and `double height`.

Renders a `Container` with full width, given height, and background color based on zone:
- `EnergyZone.peak`: `Color(0xFFFFF3E0).withOpacity(0.4)` (warm amber tint) in light mode, `Color(0xFFFF8F00).withOpacity(0.08)` in dark mode. Check brightness via `Theme.of(context).brightness`.
- `EnergyZone.low`: `Color(0xFFE8F5E9).withOpacity(0.4)` (muted sage tint) in light mode, `Color(0xFF81C784).withOpacity(0.08)` in dark mode.
- `EnergyZone.regular`: transparent (no decoration).

**3. `lib/features/planner/presentation/widgets/time_block_card.dart`**:

Stateless widget `TimeBlockCard` taking `ScheduleBlock block`, optional `bool isDragging` (default false), optional `bool isGhost` (default false).

Renders a `Container` with:
- Width: double.infinity (fills available horizontal space)
- Height: `block.height` (proportional to duration via pixelsPerMinute)
- Margin: `EdgeInsets.only(left: 56, right: 8)` (left margin accounts for hour markers)
- Decoration: `BoxDecoration` with:
  - `borderRadius: BorderRadius.circular(12)`
  - Background color based on energyLevel:
    - `high`: `context.colorScheme.primaryContainer`
    - `medium`: `context.colorScheme.secondaryContainer`
    - `low`: `context.colorScheme.tertiaryContainer`
  - If `isDragging`: elevation 8 via `boxShadow` with `context.colorScheme.shadow.withOpacity(0.3)`
  - If `isGhost`: opacity 0.3 via `Opacity` wrapper, dashed border via `Border.all(color: context.colorScheme.outline, width: 1)` and no fill

Content (inside Padding(8)):
- Title: `Text(block.title, style: context.textTheme.bodyMedium, maxLines: 1, overflow: TextOverflow.ellipsis)`
- If block.height > 40 (enough space): subtitle with time range, e.g. "9:00 AM - 10:00 AM". Format startMinute and endMinute: `final h = minute ~/ 60; final m = minute % 60; final period = h >= 12 ? 'PM' : 'AM'; final displayH = h > 12 ? h - 12 : (h == 0 ? 12 : h);` -> `"$displayH:${m.toString().padLeft(2, '0')} $period"`. Style: `context.textTheme.labelSmall`.
- If block.height > 60: show duration badge: "${block.durationMinutes} min" in a small chip.

**4. `lib/features/planner/presentation/widgets/empty_slot.dart`**:

Stateless widget `EmptySlot` taking `int hour`, `VoidCallback onTap`.

Renders a `GestureDetector` with onTap:
- `Container` with height `TimelineConstants.hourHeight`, margin `EdgeInsets.only(left: 56, right: 8)`.
- `DottedBorder`-style: Use `Container` with `BoxDecoration` having `border: Border.all(color: context.colorScheme.outlineVariant.withOpacity(0.3), width: 1)`, `borderRadius: BorderRadius.circular(8)`.
- Dashed effect: Since no dashed border package, use solid thin border with low opacity. Center a small `Icon(Icons.add_rounded, size: 20, color: context.colorScheme.onSurfaceVariant.withOpacity(0.4))`.
- Only show the empty slot if no schedule block occupies that hour. The parent (timeline_widget) handles visibility logic.

**5. `lib/features/planner/presentation/widgets/shimmer_timeline.dart`**:

Stateless widget `ShimmerTimeline`.

Renders a Column of 6-8 placeholder blocks at varying positions simulating a schedule:
- Each placeholder is a `Container` with margin `EdgeInsets.only(left: 56, right: 8, bottom: 8)`, `borderRadius: BorderRadius.circular(12)`, height varying (40, 60, 80, 100 -- representing 30min, 45min, 60min, 75min blocks).
- Background: `context.colorScheme.surfaceContainerHighest.withOpacity(0.5)`.
- Use `AnimatedOpacity` or a simple `TweenAnimationBuilder<double>` cycling opacity between 0.3 and 0.7 with a 1.5 second duration and repeat to create a subtle shimmer pulse effect. Use a `StatefulWidget` for this with an `AnimationController` set to repeat.
- OR simpler approach: wrap each placeholder in a `TweenAnimationBuilder<double>(tween: Tween(begin: 0.3, end: 0.7), duration: Duration(milliseconds: 1500), builder: ...)` -- but this won't repeat. Better: make ShimmerTimeline a StatefulWidget with SingleTickerProviderStateMixin, AnimationController(duration: 1500ms, vsync: this)..repeat(reverse: true), CurvedAnimation. Apply opacity from animation.value to each placeholder.
  </action>
  <verify>
    <automated>
      grep -c "class HourMarker" lib/features/planner/presentation/widgets/hour_marker.dart &&
      grep -c "class EnergyZoneBand" lib/features/planner/presentation/widgets/energy_zone_band.dart &&
      grep -c "class TimeBlockCard" lib/features/planner/presentation/widgets/time_block_card.dart &&
      grep -c "class EmptySlot" lib/features/planner/presentation/widgets/empty_slot.dart &&
      grep -c "class ShimmerTimeline" lib/features/planner/presentation/widgets/shimmer_timeline.dart &&
      grep -c "AnimationController" lib/features/planner/presentation/widgets/shimmer_timeline.dart
    </automated>
  </verify>
  <acceptance_criteria>
    - lib/features/planner/presentation/widgets/hour_marker.dart contains "class HourMarker"
    - lib/features/planner/presentation/widgets/hour_marker.dart contains "SizedBox(width: 48" for the left margin area
    - lib/features/planner/presentation/widgets/hour_marker.dart contains AM/PM formatting logic
    - lib/features/planner/presentation/widgets/energy_zone_band.dart contains "class EnergyZoneBand"
    - lib/features/planner/presentation/widgets/energy_zone_band.dart contains "EnergyZone.peak" and "EnergyZone.low" color branches
    - lib/features/planner/presentation/widgets/time_block_card.dart contains "class TimeBlockCard"
    - lib/features/planner/presentation/widgets/time_block_card.dart contains "block.height" for proportional sizing
    - lib/features/planner/presentation/widgets/time_block_card.dart contains "isDragging" parameter
    - lib/features/planner/presentation/widgets/time_block_card.dart contains "isGhost" parameter
    - lib/features/planner/presentation/widgets/time_block_card.dart contains "primaryContainer" or "secondaryContainer" or "tertiaryContainer" for energy-based coloring
    - lib/features/planner/presentation/widgets/empty_slot.dart contains "class EmptySlot"
    - lib/features/planner/presentation/widgets/empty_slot.dart contains "Icons.add"
    - lib/features/planner/presentation/widgets/shimmer_timeline.dart contains "class ShimmerTimeline"
    - lib/features/planner/presentation/widgets/shimmer_timeline.dart contains "AnimationController" for shimmer pulse
    - lib/features/planner/presentation/widgets/shimmer_timeline.dart contains "repeat" for looping animation
  </acceptance_criteria>
  <done>Five timeline sub-widgets created: HourMarker with AM/PM labels, EnergyZoneBand with warm amber (peak) and muted sage (low) tints, TimeBlockCard with proportional height and energy-based coloring plus drag/ghost states, EmptySlot with "+" add button, and ShimmerTimeline with animated pulse placeholder blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Planner screen, timeline container, add-item sheet, regenerate bar, and router wiring</name>
  <files>
    lib/features/planner/presentation/widgets/timeline_widget.dart,
    lib/features/planner/presentation/widgets/add_item_sheet.dart,
    lib/features/planner/presentation/widgets/regenerate_bar.dart,
    lib/features/planner/presentation/screens/planner_screen.dart,
    lib/core/router/app_router.dart
  </files>
  <read_first>
    lib/features/planner/presentation/widgets/hour_marker.dart,
    lib/features/planner/presentation/widgets/energy_zone_band.dart,
    lib/features/planner/presentation/widgets/time_block_card.dart,
    lib/features/planner/presentation/widgets/empty_slot.dart,
    lib/features/planner/presentation/widgets/shimmer_timeline.dart,
    lib/features/planner/presentation/providers/planner_provider.dart,
    lib/features/planner/presentation/providers/plannable_items_provider.dart,
    lib/features/planner/domain/timeline_constants.dart,
    lib/features/planner/domain/schedule_block_model.dart,
    lib/features/planner/domain/plannable_item_model.dart,
    lib/features/profile/domain/profile_model.dart,
    lib/features/profile/presentation/providers/profile_provider.dart,
    lib/features/auth/presentation/providers/auth_provider.dart,
    lib/core/router/app_router.dart,
    lib/shared/widgets/app_button.dart,
    lib/shared/widgets/app_text_field.dart,
    lib/core/utils/extensions.dart
  </read_first>
  <action>
Create 4 new files and update 1 existing file:

**1. `lib/features/planner/presentation/widgets/timeline_widget.dart`**:

StatelessWidget `TimelineWidget` taking: `List<ScheduleBlock> blocks`, `EnergyPattern energyPattern`, `VoidCallback onEmptySlotTap` (called when user taps an empty slot's "+" button).

Build method:
- Wrap in `SingleChildScrollView` with `ClampingScrollPhysics()`.
- Inside: `SizedBox(height: TimelineConstants.totalHeight)` containing a `Stack`.
- Stack children (back to front):
  1. **Energy zone bands**: Loop hours from `TimelineConstants.startHour` to `TimelineConstants.endHour - 1`. For each hour, create a `Positioned` at `top: TimelineConstants.minuteToY(hour * 60)`, height `TimelineConstants.hourHeight`, full width. Child: `EnergyZoneBand(zone: TimelineConstants.getZone(hour, energyPattern), height: TimelineConstants.hourHeight)`.
  2. **Faint 15-minute guide lines**: For each hour, draw 3 faint horizontal lines at 15, 30, 45 minutes within the hour. Use `Positioned` + `Container(height: 0.5, color: context.colorScheme.outlineVariant.withOpacity(0.15))` at appropriate Y offsets. Left margin 56px.
  3. **Hour markers**: Loop hours from `TimelineConstants.startHour` to `TimelineConstants.endHour`. For each, `Positioned` at `top: TimelineConstants.minuteToY(hour * 60)`. Child: `HourMarker(hour: hour)`.
  4. **Empty slots**: For each hour from startHour to endHour-1, if no block occupies that hour (check if any block's startMinute..endMinute range overlaps hour*60..(hour+1)*60), show `Positioned` at correct Y, child `EmptySlot(hour: hour, onTap: onEmptySlotTap)`.
  5. **Schedule blocks**: For each block in blocks, `Positioned` at `top: block.topOffset`, child `TimeBlockCard(block: block)`.

**2. `lib/features/planner/presentation/widgets/add_item_sheet.dart`**:

ConsumerStatefulWidget `AddItemSheet` taking `String userId`.

Form with:
- Title: `AppTextField` with label "What do you need to do?", validator non-empty, controller `_titleController`.
- Duration picker: Row of `ChoiceChip` widgets for values [15, 30, 45, 60, 90, 120]. Labels: "15m", "30m", "45m", "1h", "1.5h", "2h". Track selected value in `_selectedDuration` (int, default 30).
- Energy level: Row of 3 `ChoiceChip` widgets for EnergyLevel.values. Labels with icons: "High" (fire icon), "Medium" (bolt icon), "Low" (coffee icon). Track in `_selectedEnergy` (EnergyLevel, default medium).
- Submit button: `AppButton` with text "Add Item". On press: validate form, call `ref.read(plannableItemsProvider(userId).notifier).addItem(title: _titleController.text.trim(), durationMinutes: _selectedDuration, energyLevel: _selectedEnergy)`. Pop the bottom sheet via `Navigator.pop(context)`.

Show via `showModalBottomSheet` -- the sheet should have rounded top corners, drag handle, and respect safe area.

**3. `lib/features/planner/presentation/widgets/regenerate_bar.dart`**:

ConsumerStatefulWidget `RegenerateBar` taking `String userId`, `VoidCallback onRegenerate`.

Renders a Card with:
- Row: `AppTextField` (expanded, hint "e.g., I have a meeting at 2 PM", controller `_constraintsController`, compact height) + `IconButton(icon: Icons.refresh_rounded, onPressed: ...)`.
- On icon press: call `ref.read(plannerProvider(userId).notifier).updateConstraints(_constraintsController.text.trim().isEmpty ? null : _constraintsController.text.trim())`, then call `onRegenerate()`.
- Padding and decoration consistent with app theme.

**4. `lib/features/planner/presentation/screens/planner_screen.dart`**:

ConsumerStatefulWidget `PlannerScreen`.

State setup:
- Get current userId from auth provider (same pattern as ProfileScreen): `final userId = ref.watch(authStateProvider).user?.id ?? '';`
- Watch `plannableItemsProvider(userId)` for items.
- Watch `plannerProvider(userId)` for schedule state.
- Watch `profileProvider(userId)` for energy pattern.
- In `initState` or via ref.listen: load cached schedule on first build via `ref.read(plannerProvider(userId).notifier).loadCachedSchedule(selectedDate)`.

App bar:
- Title: "My Day" or "Daily Planner"
- Date selector: `TextButton` showing current date (e.g., "Today" or formatted date). On tap: show `showDatePicker` with first date today, last date 30 days ahead. On select: update date in plannableItemsProvider via `setDate(date)` and load cached schedule for new date.

Body -- 3 states:
1. **isGenerating == true**: Show `ShimmerTimeline()` filling the scrollable area.
2. **error != null**: Show centered Column with warning icon, friendly message ("Oops, couldn't plan your day right now. Let's try again!"), `AppButton` with "Retry" that calls generateSchedule again.
3. **Normal**: Show `TimelineWidget(blocks: state.blocks, energyPattern: profile.energyPattern, onEmptySlotTap: _showAddItemSheet)`.

Below timeline (or as a sticky bar):
- If `state.blocks.isNotEmpty`: Show `RegenerateBar(userId: userId, onRegenerate: _generate)`.

FAB:
- If `state.blocks.isEmpty && !state.isGenerating`: Large "Plan My Day" FAB. Use `FloatingActionButton.extended(icon: Icons.auto_awesome_rounded, label: Text("Plan My Day"), onPressed: _generate)`. Amber/primary color.
- If `state.blocks.isEmpty && items are also empty`: Show a welcoming empty state: "Add some items, then let AI plan your day!" with a prominent "+" add button.

Methods:
- `_generate()`: read items from plannableItemsProvider. If items empty, show SnackBar "Add some items first!". Else: read profile's energyPattern, call `ref.read(plannerProvider(userId).notifier).generateSchedule(items: items, energyPattern: energyPattern, constraints: state.constraintsText)`.
- `_showAddItemSheet()`: `showModalBottomSheet(context: context, isScrollControlled: true, builder: (_) => Padding(padding: EdgeInsets.only(bottom: MediaQuery.of(context).viewInsets.bottom), child: AddItemSheet(userId: userId)))`.

Also add a small items count badge or section: show "N items to schedule" near the top so user knows how many items exist.

**5. Update `lib/core/router/app_router.dart`**:

Add import: `import '../../features/planner/presentation/screens/planner_screen.dart';`

Replace the Planner route (lines 103-106):
```dart
GoRoute(
  path: '/planner',
  builder: (context, state) => const PlannerScreen(),
),
```

Remove the `PlaceholderTab` import if it's no longer used by any route. Check if Tasks and Habits still use it -- they do (lines 95-96 and 99-100 still reference PlaceholderTab for Tasks and Habits), so keep the import.
  </action>
  <verify>
    <automated>
      grep -c "class TimelineWidget" lib/features/planner/presentation/widgets/timeline_widget.dart &&
      grep -c "class AddItemSheet" lib/features/planner/presentation/widgets/add_item_sheet.dart &&
      grep -c "class RegenerateBar" lib/features/planner/presentation/widgets/regenerate_bar.dart &&
      grep -c "class PlannerScreen" lib/features/planner/presentation/screens/planner_screen.dart &&
      grep -c "Plan My Day" lib/features/planner/presentation/screens/planner_screen.dart &&
      grep -c "ShimmerTimeline" lib/features/planner/presentation/screens/planner_screen.dart &&
      grep -c "PlannerScreen" lib/core/router/app_router.dart &&
      grep -c "PlaceholderTab" lib/core/router/app_router.dart
    </automated>
  </verify>
  <acceptance_criteria>
    - lib/features/planner/presentation/widgets/timeline_widget.dart contains "class TimelineWidget"
    - lib/features/planner/presentation/widgets/timeline_widget.dart contains "SingleChildScrollView"
    - lib/features/planner/presentation/widgets/timeline_widget.dart contains "EnergyZoneBand"
    - lib/features/planner/presentation/widgets/timeline_widget.dart contains "HourMarker"
    - lib/features/planner/presentation/widgets/timeline_widget.dart contains "TimeBlockCard"
    - lib/features/planner/presentation/widgets/timeline_widget.dart contains "EmptySlot"
    - lib/features/planner/presentation/widgets/timeline_widget.dart contains "TimelineConstants.totalHeight"
    - lib/features/planner/presentation/widgets/add_item_sheet.dart contains "class AddItemSheet"
    - lib/features/planner/presentation/widgets/add_item_sheet.dart contains "ChoiceChip" for duration picker
    - lib/features/planner/presentation/widgets/add_item_sheet.dart contains "EnergyLevel" for energy selector
    - lib/features/planner/presentation/widgets/add_item_sheet.dart contains "plannableItemsProvider"
    - lib/features/planner/presentation/widgets/regenerate_bar.dart contains "class RegenerateBar"
    - lib/features/planner/presentation/widgets/regenerate_bar.dart contains "Icons.refresh"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "class PlannerScreen"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "Plan My Day"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "ShimmerTimeline"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "plannerProvider"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "plannableItemsProvider"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "profileProvider"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "generateSchedule"
    - lib/features/planner/presentation/screens/planner_screen.dart contains "showDatePicker" for date selection
    - lib/features/planner/presentation/screens/planner_screen.dart contains "RegenerateBar"
    - lib/features/planner/presentation/screens/planner_screen.dart contains error state handling with retry
    - lib/core/router/app_router.dart contains "PlannerScreen" (not PlaceholderTab for /planner route)
    - lib/core/router/app_router.dart does NOT contain "PlaceholderTab(title: 'Planner')" (replaced)
    - lib/core/router/app_router.dart still contains PlaceholderTab import (used by Tasks and Habits routes)
  </acceptance_criteria>
  <done>Timeline container widget renders scrollable day view with energy zones, hour markers, block cards, and empty slots. Add-item bottom sheet lets users create plannable items with title, duration, and energy level. Regenerate bar allows constraint input and re-generation. Planner screen orchestrates all states (empty, loading/shimmer, error, populated). Router updated to show PlannerScreen on the /planner tab.</done>
</task>

</tasks>

<verification>
- PlannerScreen is accessible via bottom navigation "/planner" route
- Timeline renders 16 hours (6 AM to 10 PM) with proportional block heights
- Energy zone bands use amber (peak) and sage (low) background tints
- "Plan My Day" FAB triggers AI generation with shimmer loading state
- Error state shows friendly message with retry button
- Add-item sheet has duration picker (6 options) and energy selector (3 options)
- RegenerateBar appears after initial generation
- Date picker allows selecting today or future dates
</verification>

<success_criteria>
- 10 files created/modified
- User can navigate to planner, add items, trigger AI generation, see timeline with blocks
- All 3 UI states (empty/shimmer/populated) render correctly
- Error handling shows friendly message without fallback scheduling
- Router wiring replaces PlaceholderTab for /planner route
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-daily-planner/05-02-SUMMARY.md`
</output>
