/// Supabase project configuration.
///
/// Copy this file to `supabase_constants.dart` and replace
/// the placeholder values with your actual Supabase project credentials.
///
/// NEVER commit the real `supabase_constants.dart` to version control.
class SupabaseConstants {
  SupabaseConstants._();

  static const String url = 'YOUR_SUPABASE_URL';
  static const String anonKey = 'YOUR_SUPABASE_ANON_KEY';
}
